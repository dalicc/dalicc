# What the data checks in scripts/ need, and nothing else.
#
# The checks read the Turtle in licensedata/ and the YAML review records beside it, so
# rdflib and PyYAML are the whole list. The reasoner has its own requirements files
# under reasoner/, because it runs as a service and pulls in a solver.
#
# The versions are the ones the data was validated with.

rdflib==7.6.0
PyYAML==6.0.3
